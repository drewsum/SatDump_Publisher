version: "3.8"
services:
  satdump-publisher:
    build: .
    image: satdump-publisher:local
    volumes:
      # Original incoming data (replace with your actual host path if different)
      - 'C:\Users\drewm\Documents\live_output:/data/input:ro'
      # Output (DB, generated site, thumbnails) stored in repo-local folder
      - ./output:/data
    command: ["satdump_publisher"]
    restart: "no"
